# PC GStreamer requirements for receiving streams
# Install with: sudo apt install $(cat pc_requirements.txt | grep -v '^#' | tr '\n' ' ')

gstreamer1.0-tools
gstreamer1.0-plugins-base
gstreamer1.0-plugins-good
gstreamer1.0-plugins-bad
